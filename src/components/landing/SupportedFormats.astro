---
const formats = [
  {
    name: "Excel 工作簿 (.xlsx)",
    desc:
      "兼容 Microsoft Excel 2007 及以上版本生成的标准文件。支持读取表格中所有工作表的第一张表格，完美处理中文数据和大量联系人记录。",
    icon: "xlsx",
  },
  {
    name: "旧版 Excel (.xls)",
    desc:
      "全面兼容早期版本的 Excel 文件格式 (.xls)。确保您无需进行额外的格式转换，即可直接上传和处理历史数据或从旧系统导出的文件。",
    icon: "xls",
  },
  {
    name: "逗号分隔值文件 (.csv)",
    desc:
      "最通用的数据交换格式。本工具能智能识别不同的分隔符（如逗号、制表符等）和编码（UTF-8, GBK），有效避免数据导入后的乱码问题。",
    icon: "csv",
  },
];
---

<section class="py-16">
  <div class="px-8 mx-auto md:px-12 lg:px-24 max-w-7xl">
    <div class="text-center mb-10">
      <h2 class="text-xl leading-tight tracking-tight sm:text-2xl md:text-3xl lg:text-4xl font-medium text-base-900">支持的通讯录表格文件类型</h2>
      <p class="mt-3 text-base text-base-500">本工具支持广泛的表格格式，确保您可以轻松地将您的联系人数据批量导入。所有转换过程均在本地完成，隐私安全有保障。</p>
    </div>
    <div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
      {
        formats.map((f) => (
          <div class="bg-accent-50 rounded-2xl p-8 shadow-sm ring-1 ring-base-200">
            <div class="mx-auto size-12 rounded-full bg-base-700/20 text-base-700 flex items-center justify-center">
              {f.icon === "xlsx" && (
                <svg class="size-6" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                  <rect x="4" y="3" width="16" height="18" rx="2" stroke-width="1.8"></rect>
                  <path d="M8 8l2 3-2 3M12 8l-2 3 2 3M14 8h4M14 12h4M14 16h4" stroke-width="1.8"></path>
                </svg>
              )}
              {f.icon === "xls" && (
                <svg class="size-6" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                  <rect x="4" y="3" width="16" height="18" rx="2" stroke-width="1.8"></rect>
                  <path d="M7 8h6M7 12h10M7 16h10" stroke-width="1.8"></path>
                </svg>
              )}
              {f.icon === "csv" && (
                <svg class="size-6" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                  <rect x="4" y="3" width="16" height="18" rx="2" stroke-width="1.8"></rect>
                  <path d="M7 8h10M7 12h10M7 16h10" stroke-width="1.8"></path>
                  <path d="M7 12v7M11 8v11M15 10v9" stroke-width="1.2" opacity=".6"></path>
                </svg>
              )}
            </div>
            <div class="mt-5 text-base-900 font-semibold text-lg text-center">{f.name}</div>
            <p class="mt-3 text-sm text-base-500 text-center">{f.desc}</p>
          </div>
        ))
      }
    </div>
  </div>
</section>
