---
// 1. 导入正确的模块和函数
const { t } = Astro.props;
const groups = [
  { title: t('whoUse.groups.0.title'), desc: t('whoUse.groups.0.desc') },
  { title: t('whoUse.groups.1.title'), desc: t('whoUse.groups.1.desc') },
  { title: t('whoUse.groups.2.title'), desc: t('whoUse.groups.2.desc') },
  { title: t('whoUse.groups.3.title'), desc: t('whoUse.groups.3.desc') },
];
const icons = ['💼', '🗂️', '📱', '📅'];
---

<section class="py-16">
  <div class="px-8 mx-auto md:px-12 lg:px-24 max-w-7xl">
    <div class="text-center mb-10">
      <h2 class="text-xl leading-tight tracking-tight sm:text-2xl md:text-3xl lg:text-4xl font-medium text-base-900">{t('whoUse.title')}</h2>
    </div>
    <div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-4">
      {
        groups.map((g, idx) => (
          <div class="bg-accent-50 rounded-2xl p-8 shadow-sm ring-1 ring-base-200">
            <div class="mx-auto size-12 rounded-full bg-base-700/20 text-base-700 flex items-center justify-center">
              <span class="text-2xl">{icons[idx] || '📁'}</span>
            </div>
            <div class="mt-5 text-base-900 font-semibold text-lg text-center">{g.title}</div>
            <p class="mt-3 text-sm text-base-500 text-center">{g.desc}</p>
          </div>
        ))
      }
    </div>
  </div>
</section>
